package com.example.android.books.bookDetail

import android.app.Application
import androidx.lifecycle.AndroidViewModel
import com.example.android.books.network.BookProperty

class BookDetailViewModel (@Suppress("UNUSED_PARAMETER")bookProperty: BookProperty, app: Application): AndroidViewModel(app){

}